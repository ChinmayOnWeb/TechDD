# Tooling state — not project files
.gstack/
.claude/scheduled_tasks.lock

# Python
*.egg-info/
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env

# DD reports on real target companies -- may reference third-party security
# findings (e.g. secrets in their git history); not for public disclosure.
reports/

# Panel-study per-run artifacts (rebuildable from cache; published dataset
# committed deliberately once the universe is final).
panel_cache/
panel.csv
panel_results/

# Part C cohort working data (regenerable from the frozen frame + seed)
cohort_cache/
cohort_work/
cohort_results/
